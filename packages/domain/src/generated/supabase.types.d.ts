// Placeholder for generated Supabase types.
// After running `supabase gen types typescript --project-id <id> --schema public > supabase.types.ts`
// we will re-export the Database interface here for use across packages.
// This file exists now so imports won't break once we wire generation.

export interface DatabasePlaceholder {
  public: {
    Tables: Record<string, unknown>;
  };
}
