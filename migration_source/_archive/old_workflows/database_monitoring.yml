name: Deprecated - Database Monitoring

on:
  workflow_dispatch:

env:
  NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
  NEON_PROJECT_ID: ${{ secrets.NEON_PROJECT_ID }}
  DATABASE_URL: ${{ secrets.NETLIFY_DATABASE_URL }}
  NETLIFY_DATABASE_URL: ${{ secrets.NETLIFY_DATABASE_URL }}
  NETLIFY_DATABASE_URL_UNPOOLED: ${{ secrets.NETLIFY_DATABASE_URL_UNPOOLED }}
  DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated and kept for history. Database now managed by Supabase/managed services."
